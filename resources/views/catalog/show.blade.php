@extends('layouts.app')
@section('title', $item->name.' | Armour')
@section('content')
<section class="section detail-section"><div class="container"><a class="text-link" href="{{ route('home') }}#packages">← Back to packages</a><div class="detail-layout"><div><div class="catalog-image detail-image" data-image-zoom tabindex="0" aria-label="Zoom image of {{ $item->name }}">@if($item->image_url)<img src="{{ $item->image_url }}" alt="{{ $item->image_alt ?: $item->name }}" width="800" height="600"><span class="image-zoom-lens" aria-hidden="true"></span><span class="image-zoom-hint" aria-hidden="true">⌕ Hover to zoom</span>@else<span>Photo coming soon</span>@endif</div>@if($item->is_sample_image)<p class="data-note">Sample image for illustration. Actual item and inclusions may differ. @if($item->image_source)<a href="{{ $item->image_source }}" target="_blank" rel="noopener">Image source ↗</a>@endif</p>@endif</div><div><p class="eyebrow">{{ $item->category }}</p><h1>{{ $item->name }}</h1><p class="detail-spec">{{ $item->specification }}</p><p class="detail-description">{{ $item->description }}</p>@if($item->inclusions)<h2>Package inclusions</h2><ul class="detail-inclusions">@foreach($item->inclusions as $inclusion)<li>{{ $inclusion }}</li>@endforeach</ul>@endif<p class="data-note">Confirm vehicle compatibility, availability, and final installation pricing with your branch.</p>@include('partials.catalog-cta')</div></div></div></section>
@endsection
